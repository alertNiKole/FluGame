 players.forEach(i = 0; i > players.length; i ++) => {
        

    //Start msg
    let startGame = document.querySelector("#start"); //change to listener if needed
    let startMsg = document.querySelector("#welcome");
    let startButton = startGame.addEventListener("click", () => {
        startMsg.innerHTML= "The game is afoot!  <br> "
        gameLogic();
    });


//Jenna code

        <section class="jenna" hidden>
            <h1>Jennas code</h1>
            <span id="p1Display">0</span> to <span id="p2Display">0</span></h1>

            <p>Playing to: 5</p>
            <input type="number">
            <button id="p1">Player One</button>
            <button id="p2">Player Two</button>
            <button id="reset">Reset</button>
        </section>
            <hr>
            <hr>




    JS

     // Check Score Button
    /*  
        name button
        select button
        listen
        do something */
        /* Does not execute -->    checkScore= document.querySelector("#score");  */
        /*
        checkScore= document.querySelector("#score");
        scoreMsg = document.querySelector("#scoreText");
        
        scoreButton = checkScore.addEventListener("click", () => {
            
            scoreMsg.innerHTML = "Its flu season and our players are trying to stay healthy. <br> <strong class='accent t-shadow'> The Human has coughed:  " + playerNumOfCoughs + "  | Our Computer has coughed:  " + compNumOfCoughs + '</strong>' ;
               }) */



third logic stream

         
roll = rng(7);
//pass roll to the first day sum

nextDayRoll = roll;
console.log("Next Roll was.",nextDayRoll );

let stored = parseInt(playerTotalText.innerHTML);

console.log("stored value:", stored, "nextDayRoll", nextDayRoll ); 

playerTotalText.innerHTML = stored + nextDayRoll;



Do while text - working, bad calculations


                        //next day calculation
                        do {
                        dayCounter++;
                        console.log("Day counter is", dayCounter, "and the day count is: ", dayCount);
                          roll = parseInt(pCurrentScore.innerHTML)//4
                          console.log("roll: ", roll);

                          current= roll; //4
                          console.log("current: ", current);

                          temp = roll; //4
                          console.log("temp: ", temp);

                        
                        stored =  firstDayTotal + roll; 
                        console.log("stored: ", stored, "<br>"); //3

                        total = stored;

                        total = stored+temp;   
                        console.log("Total: ", total); //7

                      //console msg
                      console.log(
                        "rolled: ",  rolled,
                        "current: ", current,
                        "temp: ",   temp,
                        "stored: ", stored,
                        "total:",     total,
                        "storage:", storage);
                      };

                      let sum = "";
                      sum  = (current + roll) + sum;

                      while(dayCounter >= daysInFluSeason);
                        //change wording on button
                        //next roll formula
                
                        
    